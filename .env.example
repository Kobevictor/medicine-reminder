# ============================================================
# Medicine Reminder - 环境变量配置
# 使用方法：复制此文件为 .env 并修改配置值
#   cp .env.example .env
# ============================================================

# ---- 数据库配置 ----
# 本地开发时使用（直接连接本地MySQL）
DATABASE_URL=mysql://medicine_user:medicine_password@localhost:3306/medicine_reminder

# Docker Compose 使用的MySQL配置
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=medicine_reminder
MYSQL_USER=medicine_user
MYSQL_PASSWORD=medicine_password

# ---- 安全配置 ----
# JWT密钥（生产环境务必修改为随机长字符串！）
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=please-change-this-secret-key

# ---- 服务端口 ----
# 应用端口（Docker和本地开发通用）
PORT=3000
APP_PORT=3000

# 数据库端口映射（仅Docker使用，可选修改）
DB_PORT=3306

# ---- 运行环境 ----
NODE_ENV=production
