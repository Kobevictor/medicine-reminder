services:
  # MySQL数据库服务
  db:
    image: mysql:8.0
    container_name: medicine-reminder-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-medicine_reminder}
      MYSQL_USER: ${MYSQL_USER:-medicine_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-medicine_password}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "${DB_PORT:-3306}:3306"
    networks:
      - medicine-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-rootpassword}"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  # 应用服务
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medicine-reminder-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_URL: mysql://${MYSQL_USER:-medicine_user}:${MYSQL_PASSWORD:-medicine_password}@db:3306/${MYSQL_DATABASE:-medicine_reminder}
      JWT_SECRET: ${JWT_SECRET:-please-change-this-secret-key}
      PORT: 3000
    ports:
      - "${APP_PORT:-3000}:3000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - medicine-network

volumes:
  mysql_data:
    driver: local

networks:
  medicine-network:
    driver: bridge
