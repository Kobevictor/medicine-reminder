# 药物提醒系统 - 安装与配置指南

本文档提供药物提醒系统的完整安装、配置和使用说明。

## 📋 目录

- [系统概述](#系统概述)
- [环境要求](#环境要求)
- [安装步骤](#安装步骤)
- [配置说明](#配置说明)
- [使用指南](#使用指南)
- [常见问题](#常见问题)

## 系统概述

药物提醒系统是一个基于 Web 的应用程序，帮助用户管理药物服用计划，记录服药历史，并在药物库存不足时通知家人。

### 主要特性

- ✅ **本地用户认证**：已移除 Manus OAuth，改用用户名/密码登录
- ✅ **药物管理**：添加、编辑、删除药物信息
- ✅ **服药记录**：记录每次服药情况
- ✅ **家人通知**：药物库存不足时自动通知
- ✅ **邮件提醒**：支持自定义 SMTP 配置
- ✅ **Docker 部署**：一键部署，包含完整环境

### 技术架构

```
┌─────────────────────────────────────────────┐
│           用户浏览器 (Browser)               │
└─────────────────┬───────────────────────────┘
                  │ HTTP/HTTPS
                  ▼
┌─────────────────────────────────────────────┐
│        应用服务器 (Node.js + Express)        │
│  - React 前端 (静态文件)                     │
│  - tRPC API                                  │
│  - JWT 认证                                  │
└─────────────────┬───────────────────────────┘
                  │ MySQL Protocol
                  ▼
┌─────────────────────────────────────────────┐
│          数据库 (MySQL 8.0)                  │
│  - 用户数据                                  │
│  - 药物信息                                  │
│  - 服药记录                                  │
└─────────────────────────────────────────────┘
```

## 环境要求

### 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 1核心 | 2核心或以上 |
| 内存 | 1GB | 2GB或以上 |
| 磁盘 | 3GB | 10GB或以上 |
| 网络 | 1Mbps | 10Mbps或以上 |

### 软件要求

#### 使用 Docker 部署（推荐）

- **操作系统**：Linux / macOS / Windows (with WSL2)
- **Docker**：20.10.0 或更高版本
- **Docker Compose**：2.0.0 或更高版本

#### 本地开发部署

- **操作系统**：Linux / macOS / Windows
- **Node.js**：22.x
- **pnpm**：10.x
- **MySQL**：8.0

## 安装步骤

### 方式一：Docker 一键部署（推荐）

#### 1. 安装 Docker

**Linux (Ubuntu/Debian):**

```bash
# 安装 Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 将当前用户添加到 docker 组
sudo usermod -aG docker $USER

# 重新登录以使组权限生效
newgrp docker

# 验证安装
docker --version
docker compose version
```

**macOS:**

```bash
# 使用 Homebrew
brew install --cask docker

# 或下载 Docker Desktop
# https://www.docker.com/products/docker-desktop
```

**Windows:**

下载并安装 Docker Desktop for Windows:
https://www.docker.com/products/docker-desktop

#### 2. 克隆项目

```bash
git clone https://github.com/Kobevictor/medicine-reminder.git
cd medicine-reminder
```

#### 3. 配置环境变量

```bash
# 复制示例配置
cp .env.example .env

# 编辑配置文件
nano .env  # 或使用其他编辑器
```

**必须修改的配置项：**

```env
# JWT密钥（务必修改为随机字符串）
JWT_SECRET=请修改为一个长且随机的字符串

# 数据库密码（建议修改）
MYSQL_ROOT_PASSWORD=your_secure_root_password
MYSQL_PASSWORD=your_secure_database_password
```

**生成随机 JWT_SECRET 的方法：**

```bash
# Linux/macOS
openssl rand -base64 32

# 或使用 Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

#### 4. 启动服务

**使用快速启动脚本：**

```bash
./start.sh
```

**或手动启动：**

```bash
# 构建并启动容器
docker compose up -d

# 查看日志
docker compose logs -f

# 初始化数据库
docker compose exec app pnpm run db:push
```

#### 5. 访问应用

打开浏览器访问：http://localhost:3000

### 方式二：本地开发部署

#### 1. 安装依赖

**安装 Node.js 22.x:**

```bash
# 使用 nvm (推荐)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 22
nvm use 22

# 安装 pnpm
npm install -g pnpm
```

**安装 MySQL 8.0:**

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install mysql-server

# macOS
brew install mysql

# 启动 MySQL
sudo systemctl start mysql  # Linux
brew services start mysql   # macOS
```

#### 2. 克隆项目

```bash
git clone https://github.com/Kobevictor/medicine-reminder.git
cd medicine-reminder
```

#### 3. 安装项目依赖

```bash
pnpm install
```

#### 4. 配置数据库

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库和用户
CREATE DATABASE medicine_reminder;
CREATE USER 'medicine_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON medicine_reminder.* TO 'medicine_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### 5. 配置环境变量

```bash
cp .env.example .env
nano .env
```

修改数据库连接信息：

```env
DATABASE_URL=mysql://medicine_user:your_password@localhost:3306/medicine_reminder
JWT_SECRET=your_random_secret_key
```

#### 6. 初始化数据库

```bash
pnpm run db:push
```

#### 7. 启动应用

**开发模式：**

```bash
pnpm run dev
```

**生产模式：**

```bash
# 构建
pnpm run build

# 启动
pnpm start
```

#### 8. 访问应用

打开浏览器访问：http://localhost:3000

## 配置说明

### 环境变量详解

| 变量名 | 说明 | 默认值 | 是否必须 |
|--------|------|--------|----------|
| `DATABASE_URL` | MySQL数据库连接字符串 | - | ✅ 是 |
| `JWT_SECRET` | JWT加密密钥 | - | ✅ 是 |
| `PORT` | 应用服务端口 | 3000 | ❌ 否 |
| `NODE_ENV` | 运行环境 | development | ❌ 否 |
| `MYSQL_ROOT_PASSWORD` | MySQL root密码 | - | ✅ 是（Docker） |
| `MYSQL_DATABASE` | 数据库名称 | medicine_reminder | ❌ 否 |
| `MYSQL_USER` | 数据库用户名 | medicine_user | ❌ 否 |
| `MYSQL_PASSWORD` | 数据库密码 | - | ✅ 是（Docker） |

### 数据库连接字符串格式

```
mysql://[用户名]:[密码]@[主机]:[端口]/[数据库名]
```

**示例：**

```env
# 本地开发
DATABASE_URL=mysql://medicine_user:password123@localhost:3306/medicine_reminder

# Docker 容器
DATABASE_URL=mysql://medicine_user:password123@db:3306/medicine_reminder

# 远程数据库
DATABASE_URL=mysql://user:pass@192.168.1.100:3306/medicine_reminder
```

### SMTP 邮件配置

系统支持通过 SMTP 发送邮件通知。配置方法：

1. 登录系统后，进入"邮件设置"页面
2. 填写 SMTP 服务器信息：
   - SMTP 服务器地址（如：smtp.gmail.com）
   - SMTP 端口（465 for SSL, 587 for TLS）
   - SMTP 用户名（邮箱地址）
   - SMTP 密码（邮箱密码或授权码）
3. 点击"测试配置"验证设置
4. 保存配置

**常用邮箱 SMTP 配置：**

| 邮箱服务 | SMTP地址 | 端口 | 安全类型 |
|---------|---------|------|---------|
| Gmail | smtp.gmail.com | 587 | TLS |
| QQ邮箱 | smtp.qq.com | 465 | SSL |
| 163邮箱 | smtp.163.com | 465 | SSL |
| Outlook | smtp.office365.com | 587 | TLS |

## 使用指南

### 首次使用

#### 1. 注册账号

1. 访问 http://localhost:3000
2. 点击"注册"标签
3. 填写用户名、密码等信息
4. 点击"注册"按钮
5. 注册成功后自动登录

#### 2. 添加药物

1. 登录后进入"药物管理"页面
2. 点击"添加药物"按钮
3. 填写药物信息：
   - 药物名称
   - 剂量（如：1片、5ml）
   - 服用频率（如：每日3次）
   - 提醒时间（如：08:00, 12:00, 18:00）
   - 总量和剩余量
4. 保存

#### 3. 记录服药

1. 在首页查看今日服药计划
2. 点击"已服用"按钮记录
3. 系统自动扣减剩余量

#### 4. 添加家人联系人

1. 进入"家人管理"页面
2. 点击"添加家人"
3. 填写联系人信息和通知偏好
4. 保存

#### 5. 配置邮件通知

1. 进入"邮件设置"页面
2. 配置 SMTP 服务器信息
3. 测试配置是否正确
4. 保存配置

### 日常使用

#### 查看服药计划

- 首页显示今日所有服药计划
- 按时间排序
- 显示已服用和未服用状态

#### 查看服药历史

- 进入"服药记录"页面
- 查看历史记录
- 支持按药物和日期筛选

#### 药物库存管理

- 系统自动计算剩余天数
- 库存不足时显示预警
- 支持手动补充库存

#### 通知管理

- 查看所有通知消息
- 标记已读/未读
- 自动发送邮件通知

## 常见问题

### 安装相关

**Q: Docker 安装失败怎么办？**

A: 请参考 Docker 官方文档：https://docs.docker.com/get-docker/

**Q: 端口 3000 被占用怎么办？**

A: 修改 `docker-compose.yml` 中的端口映射：

```yaml
app:
  ports:
    - "8080:3000"  # 改为 8080 或其他端口
```

**Q: 数据库连接失败？**

A: 检查以下几点：
1. MySQL 服务是否启动
2. 数据库连接字符串是否正确
3. 用户名和密码是否正确
4. 数据库是否已创建

### 使用相关

**Q: 忘记密码怎么办？**

A: 目前系统不支持密码重置功能。如需重置，请联系管理员直接修改数据库。

**Q: 如何修改用户信息？**

A: 登录后点击右上角用户菜单，选择"个人设置"。

**Q: 邮件通知不工作？**

A: 检查以下几点：
1. SMTP 配置是否正确
2. 邮箱是否开启了 SMTP 服务
3. 是否使用了正确的授权码（而非登录密码）
4. 防火墙是否阻止了 SMTP 端口

**Q: 如何备份数据？**

A: 使用以下命令备份数据库：

```bash
# Docker 部署
docker compose exec db mysqldump -u root -p${MYSQL_ROOT_PASSWORD} medicine_reminder > backup.sql

# 本地部署
mysqldump -u medicine_user -p medicine_reminder > backup.sql
```

**Q: 如何恢复数据？**

A: 使用以下命令恢复数据库：

```bash
# Docker 部署
docker compose exec -T db mysql -u root -p${MYSQL_ROOT_PASSWORD} medicine_reminder < backup.sql

# 本地部署
mysql -u medicine_user -p medicine_reminder < backup.sql
```

### 性能相关

**Q: 系统运行缓慢怎么办？**

A: 尝试以下优化：
1. 增加服务器内存
2. 优化 MySQL 配置
3. 清理旧的服药记录
4. 使用 SSD 存储

**Q: 如何查看系统日志？**

A: 使用以下命令：

```bash
# Docker 部署
docker compose logs -f app

# 本地部署
# 查看应用输出
```

### 安全相关

**Q: 如何增强系统安全性？**

A: 建议采取以下措施：
1. 使用强密码
2. 定期更换 JWT_SECRET
3. 启用 HTTPS
4. 配置防火墙
5. 定期更新系统
6. 定期备份数据

**Q: 如何启用 HTTPS？**

A: 使用 Nginx 作为反向代理，配置 SSL 证书。详见 [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md)。

## 技术支持

如需帮助，请：

1. 查看 [README.md](./README.md)
2. 查看 [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md)
3. 访问 GitHub Issues: https://github.com/Kobevictor/medicine-reminder/issues
4. 提交新的 Issue 描述问题

## 更新日志

### v1.0.0 (2024-02-12)

- ✅ 移除 Manus OAuth 认证
- ✅ 实现本地用户名/密码登录
- ✅ 添加用户注册功能
- ✅ 使用 bcrypt 加密密码
- ✅ 创建 Docker 部署配置
- ✅ 编写完整的部署文档

## 许可证

本项目采用 MIT 许可证。详见 [LICENSE](./LICENSE) 文件。
